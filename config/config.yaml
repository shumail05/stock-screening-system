api:
  broker: "fyers"
  client_id: "${FYERS_CLIENT_ID}"
  secret_key: "${FYERS_SECRET_KEY}"
  redirect_uri: "http://localhost:8080"
  access_token: "${FYERS_ACCESS_TOKEN}"

screening:
  price_min: 30
  price_max: 500
  min_bid_qty: 1000000
  min_ask_qty: 1000000

smma:
  fast_period: 20
  slow_period: 120

timeframes:
  quantity_windows: [5, 20, 60]
  avg_price_windows: [20, 60]
